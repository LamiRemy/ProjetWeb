{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ title }}</h1>
    <br>

    {{ form_start(form) }}
         {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}

    <a href="{{ path('connexion') }}">Je veux me connecter</a>
    <br>
    <br>
    <a href="{{ path('homepage') }}">Retournez à la page d'acceuil</a>
{% endblock %}