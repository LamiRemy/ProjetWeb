{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ title }}</h1>
    <a href="{{ path('inscription') }}">Je veux m'inscrire</a>
    <br>
    <br>
    <a href="{{ path('homepage') }}">Retournez à la page d'acceuil</a>
{% endblock %}